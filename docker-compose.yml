services:

  backend_app:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"

  celery:
    build: .
    command: celery -A project_core worker -l INFO

  app_queue:
    build: .
    command: python consumer.py